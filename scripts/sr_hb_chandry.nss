#include "NW_I0_GENERIC"

void main()
{
    // Going out for the night for Chandry.
/*    if(GetTimeHour()==19)
    {
        ActionSpeakString("I am going out for the night!  See you all later!");
        AssignCommand(OBJECT_SELF, ActionMoveToObject(GetObjectByTag("07f_goingout")));
        DelayCommand(10.0, DestroyObject(OBJECT_SELF));
    } */
}
