#include "NW_I0_GENERIC"

void main()
{
    // Coming home for the night for Chandry.
/*    if(GetTimeHour()==0)
    {
        CreateObject(OBJECT_TYPE_CREATURE, "Chandry",
                         GetLocation(GetObjectByTag("07f_goingout")));

        AssignCommand(OBJECT_SELF, ActionMoveToObject(GetObjectByTag("07f_cominghome")));
        // go to sleep?
        ActionWait(20.0);
        ActionPlayAnimation(ANIMATION_LOOPING_SIT_CROSS, 1.0, 1900.0);
    } */
}
